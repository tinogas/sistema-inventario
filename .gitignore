# Configuración local (credenciales de BD)
config/database.php

# Archivos subidos por usuarios
uploads/cfdi/*.xml

# Setup (solo usar en instalación, no en producción)
setup.php

# Logs y caché
*.log
*.cache

# Variables de entorno
.env
.env.local

# Editores e IDEs
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
.DS_Store

# Dependencias (si se agrega Composer en el futuro)
vendor/

# Fotos subidas (conservar carpeta, ignorar contenido)
uploads/fotos/*
!uploads/fotos/.gitkeep

# Archivos de respaldo de BD (no versionar)
backups/*.sql
